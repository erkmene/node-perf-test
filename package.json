{
  "name": "perf-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "cross-env TZ=utc DEBUG=perf-test* NODE_ENV=dev NODE_PATH=. LOG_LEVEL=debug nodemon --inspect=0.0.0.0:8001 --watch ./",
    "start": "DEBUG=perf-test* NODE_ENV=dev LOG_LEVEL=debug node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "ejs": "^3.1.6",
    "express": "^4.17.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.12"
  }
}
